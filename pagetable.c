#include "pagetable.h"
#include <stdio.h>
#include <stdlib.h>

pagetable *initPtTable() {
}
PtEntry *createEntry(int address) {
}
int insertToPt(pagetable pt, PtEntry currEntry) {
}
int removeFromPt(pagetable pt) {
}